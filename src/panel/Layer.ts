export interface Layer {
    id: string;
    imageData: string;
    naturalWidth: number;
    naturalHeight: number;
    viewPortX: number;
    viewPortY: number;
    viewPortWidth: number;
    viewPortHeight: number;
}